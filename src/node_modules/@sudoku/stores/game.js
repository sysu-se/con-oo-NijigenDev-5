import { derived, writable } from 'svelte/store';
import { domainStore } from './domain';

export const gamePaused = writable(true);

// 游戏胜利判断移交给 domainStore
export const gameWon = derived(domainStore, $domain => $domain.isSolved);